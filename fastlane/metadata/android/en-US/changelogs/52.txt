The GitHub update check is now off unless you switch it on. Updates taken that way come straight from the developer and are not reviewed by the app store you installed from, so the app asks once, on an early launch, and puts the switch in settings. Every update offer repeats where the file comes from before anything is downloaded. Store listing text, screenshots and the app icon now live in the repository.
